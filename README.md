Terraform AWS EC2 Webserver Project
📌 Overview

This project uses Terraform to provision a complete EC2-based web server infrastructure on AWS, including:

SSH Key Pair
Security Group with inbound/outbound rules
EC2 Instance

The configuration is modular and uses variables for flexibility across environments.
